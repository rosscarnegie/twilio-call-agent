{
  "name": "twilio-call-agent",
  "version": "1.0.0",
  "description": "A Twilio call agent that can be used to handle incoming calls and make outgoing calls",
  "main": "src/server.ts",
  "scripts": {
    "tsc": "tsc",
    "start": "ts-node-dev -r tsconfig-paths/register --transpile-only --no-deps ./src/server.ts",
    "prod": "tsc && node -r module-alias/register build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "ivr",
    "twilio",
    "express",
    "twiml",
    "webhooks"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cookie-parser": "^1.4.6",
    "debug": "^4.3.4",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "jade": "^1.11.0",
    "serve-favicon": "^2.5.0",
    "stylus": "^0.62.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "twilio": "^4.21.0",
    "typescript": "^5.3.3"
  }
}
